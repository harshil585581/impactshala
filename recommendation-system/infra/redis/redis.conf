bind 0.0.0.0
protected-mode no
port 6379

# Persistence
save 900 1
save 300 10
save 60 10000
appendonly yes
appendfsync everysec

# Memory limit (adjust to your host)
maxmemory 512mb
maxmemory-policy allkeys-lru

# Streams
stream-node-max-bytes 4096
stream-node-max-entries 100
